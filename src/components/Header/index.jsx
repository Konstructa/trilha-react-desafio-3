import P from 'prop-types';
import * as Styled from './styles';

export const Header = () => {
  return (
    <Styled.Header>
        
    </Styled.Header>
  )
}

Header.propTypes = {
    children: P.node.isRequired,
}